<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/22 11:31:15 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.lolluckyman.business.team.dao.ITeamDao">

    <resultMap type="com.lolluckyman.business.team.entity.Team" id="resultMap">
        <id property="code" column="code" />
        <result property="chinaName" column="chinaname" />
        <result property="englishName" column="englishname" />
        <result property="teamTeacher" column="teamteacher" />
        <result property="teamWeb" column="teamweb" />
        <result property="createTeamTime" column="createteamtime" />
        <result property="description" column="description" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        code, chinaname, englishname, teamteacher, teamweb, createteamtime, description, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="code != null"><![CDATA[ and code like #{code} ]]></if>
            <if test="code_enum != null">
                <![CDATA[ and code in ]]>
                <foreach collection="code_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="chinaName != null"><![CDATA[ and chinaname like #{chinaName} ]]></if>
            <if test="chinaName_enum != null">
                <![CDATA[ and chinaname in ]]>
                <foreach collection="chinaName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="englishName != null"><![CDATA[ and englishname like #{englishName} ]]></if>
            <if test="englishName_enum != null">
                <![CDATA[ and englishname in ]]>
                <foreach collection="englishName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="teamTeacher != null"><![CDATA[ and teamteacher like #{teamTeacher} ]]></if>
            <if test="teamTeacher_enum != null">
                <![CDATA[ and teamteacher in ]]>
                <foreach collection="teamTeacher_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="teamWeb != null"><![CDATA[ and teamweb like #{teamWeb} ]]></if>
            <if test="teamWeb_enum != null">
                <![CDATA[ and teamweb in ]]>
                <foreach collection="teamWeb_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTeamTime != null"><![CDATA[ and createteamtime = #{createTeamTime} ]]></if>
            <if test="createTeamTime_r_min != null"><![CDATA[ and createteamtime >= #{createTeamTime_r_min} and createteamtime < #{createTeamTime_r_max} ]]></if>
            <if test="description != null"><![CDATA[ and description like #{description} ]]></if>
            <if test="description_enum != null">
                <![CDATA[ and description in ]]>
                <foreach collection="description_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'code'"><![CDATA[ code like #{item.value} ]]></if>
                    <if test="item.name == 'chinaName'"><![CDATA[ chinaname like #{item.value} ]]></if>
                    <if test="item.name == 'englishName'"><![CDATA[ englishname like #{item.value} ]]></if>
                    <if test="item.name == 'teamTeacher'"><![CDATA[ teamteacher like #{item.value} ]]></if>
                    <if test="item.name == 'teamWeb'"><![CDATA[ teamweb like #{item.value} ]]></if>
                    <if test="item.name == 'description'"><![CDATA[ description like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'code_desc'"><![CDATA[ code desc, ]]></if>
            <if test="_orderBy == 'code_asc'"><![CDATA[ code, ]]></if>
            <if test="_orderBy == 'chinaName_desc'"><![CDATA[ chinaname desc, ]]></if>
            <if test="_orderBy == 'chinaName_asc'"><![CDATA[ chinaname, ]]></if>
            <if test="_orderBy == 'englishName_desc'"><![CDATA[ englishname desc, ]]></if>
            <if test="_orderBy == 'englishName_asc'"><![CDATA[ englishname, ]]></if>
            <if test="_orderBy == 'teamTeacher_desc'"><![CDATA[ teamteacher desc, ]]></if>
            <if test="_orderBy == 'teamTeacher_asc'"><![CDATA[ teamteacher, ]]></if>
            <if test="_orderBy == 'teamWeb_desc'"><![CDATA[ teamweb desc, ]]></if>
            <if test="_orderBy == 'teamWeb_asc'"><![CDATA[ teamweb, ]]></if>
            <if test="_orderBy == 'createTeamTime_desc'"><![CDATA[ createteamtime desc, ]]></if>
            <if test="_orderBy == 'createTeamTime_asc'"><![CDATA[ createteamtime, ]]></if>
            <if test="_orderBy == 'description_desc'"><![CDATA[ description desc, ]]></if>
            <if test="_orderBy == 'description_asc'"><![CDATA[ description, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.lolluckyman.business.team.entity.Team">
        insert into
        lol_team(code,chinaname,englishname,teamteacher,teamweb,createteamtime,description,createtime)
        values(#{code},#{chinaName},#{englishName},#{teamTeacher},#{teamWeb},#{createTeamTime},#{description},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.lolluckyman.business.team.entity.Team">
        update lol_team
        set chinaname=#{chinaName},englishname=#{englishName},teamteacher=#{teamTeacher},teamweb=#{teamWeb},createteamtime=#{createTeamTime},description=#{description},createtime=#{createTime}
        where code=#{code};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from lol_team where code=#{code};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.lolluckyman.utils.core.QueryParams">
        delete from lol_team
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_team where code=#{code};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from lol_team where code=#{code};
    </select>

    <select id="queryCount" parameterType="com.lolluckyman.utils.core.QueryParams" resultType="int">
        select count(*) from lol_team
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_team
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select * from lol_team
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


