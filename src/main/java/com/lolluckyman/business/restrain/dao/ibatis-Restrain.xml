<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/22 19:05:55 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.lolluckyman.business.restrain.dao.IRestrainDao">

    <resultMap type="com.lolluckyman.business.restrain.entity.Restrain" id="resultMap">
        <id property="code" column="code" />
        <result property="competitionCode" column="competitioncode" />
        <result property="restrainNum" column="restrainnum" />
        <result property="restrainStatus" column="restrainstatus" />
        <result property="description" column="description" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        code, competitioncode, restrainnum, restrainstatus, description, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="code != null"><![CDATA[ and code like #{code} ]]></if>
            <if test="code_enum != null">
                <![CDATA[ and code in ]]>
                <foreach collection="code_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="competitionCode != null"><![CDATA[ and competitioncode like #{competitionCode} ]]></if>
            <if test="competitionCode_enum != null">
                <![CDATA[ and competitioncode in ]]>
                <foreach collection="competitionCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="restrainNum != null"><![CDATA[ and restrainnum = #{restrainNum} ]]></if>
            <if test="restrainNum_r_min != null"><![CDATA[ and restrainnum >= #{restrainNum_r_min} and restrainnum <= #{restrainNum_r_max} ]]></if>
            <if test="restrainNum_enum != null">
                <![CDATA[ and restrainnum in ]]>
                <foreach collection="restrainNum_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="restrainStatus != null"><![CDATA[ and restrainstatus like #{restrainStatus} ]]></if>
            <if test="restrainStatus_enum != null">
                <![CDATA[ and restrainstatus in ]]>
                <foreach collection="restrainStatus_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="description != null"><![CDATA[ and description like #{description} ]]></if>
            <if test="description_enum != null">
                <![CDATA[ and description in ]]>
                <foreach collection="description_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'code'"><![CDATA[ code like #{item.value} ]]></if>
                    <if test="item.name == 'competitionCode'"><![CDATA[ competitioncode like #{item.value} ]]></if>
                    <if test="item.name == 'restrainStatus'"><![CDATA[ restrainstatus like #{item.value} ]]></if>
                    <if test="item.name == 'description'"><![CDATA[ description like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'code_desc'"><![CDATA[ code desc, ]]></if>
            <if test="_orderBy == 'code_asc'"><![CDATA[ code, ]]></if>
            <if test="_orderBy == 'competitionCode_desc'"><![CDATA[ competitioncode desc, ]]></if>
            <if test="_orderBy == 'competitionCode_asc'"><![CDATA[ competitioncode, ]]></if>
            <if test="_orderBy == 'restrainNum_desc'"><![CDATA[ restrainnum desc, ]]></if>
            <if test="_orderBy == 'restrainNum_asc'"><![CDATA[ restrainnum, ]]></if>
            <if test="_orderBy == 'restrainStatus_desc'"><![CDATA[ restrainstatus desc, ]]></if>
            <if test="_orderBy == 'restrainStatus_asc'"><![CDATA[ restrainstatus, ]]></if>
            <if test="_orderBy == 'description_desc'"><![CDATA[ description desc, ]]></if>
            <if test="_orderBy == 'description_asc'"><![CDATA[ description, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.lolluckyman.business.restrain.entity.Restrain">
        insert into
        lol_restrain(code,competitioncode,restrainnum,restrainstatus,description,createtime)
        values(#{code},#{competitionCode},#{restrainNum},#{restrainStatus},#{description},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.lolluckyman.business.restrain.entity.Restrain">
        update lol_restrain
        set competitioncode=#{competitionCode},restrainnum=#{restrainNum},restrainstatus=#{restrainStatus},description=#{description},createtime=#{createTime}
        where code=#{code};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from lol_restrain where code=#{code};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.lolluckyman.utils.core.QueryParams">
        delete from lol_restrain
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_restrain where code=#{code};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from lol_restrain where code=#{code};
    </select>

    <select id="queryCount" parameterType="com.lolluckyman.utils.core.QueryParams" resultType="int">
        select count(*) from lol_restrain
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_restrain
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select * from lol_restrain
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


