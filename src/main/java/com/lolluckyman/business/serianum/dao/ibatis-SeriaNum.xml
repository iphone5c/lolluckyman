<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/11 11:55:03 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.lolluckyman.business.serianum.dao.ISeriaNumDao">

    <resultMap type="com.lolluckyman.business.serianum.entity.SeriaNum" id="resultMap">
        <id property="keyName" column="keyname" />
        <result property="keyValue" column="keyvalue" />
    </resultMap>

    <sql id="baseCol">
        keyname, keyvalue
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="keyName != null"><![CDATA[ and keyname like #{keyName} ]]></if>
            <if test="keyName_enum != null">
                <![CDATA[ and keyname in ]]>
                <foreach collection="keyName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="keyValue != null"><![CDATA[ and keyvalue = #{keyValue} ]]></if>
            <if test="keyValue_r_min != null"><![CDATA[ and keyvalue >= #{keyValue_r_min} and keyvalue <= #{keyValue_r_max} ]]></if>
            <if test="keyValue_enum != null">
                <![CDATA[ and keyvalue in ]]>
                <foreach collection="keyValue_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'keyName'"><![CDATA[ keyname like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'keyName_desc'"><![CDATA[ keyname desc, ]]></if>
            <if test="_orderBy == 'keyName_asc'"><![CDATA[ keyname, ]]></if>
            <if test="_orderBy == 'keyValue_desc'"><![CDATA[ keyvalue desc, ]]></if>
            <if test="_orderBy == 'keyValue_asc'"><![CDATA[ keyvalue, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.lolluckyman.business.serianum.entity.SeriaNum">
        insert into
        lol_serianum(keyname,keyvalue)
        values(#{keyName},#{keyValue});
    </insert>

    <update id="updateObject" parameterType="com.lolluckyman.business.serianum.entity.SeriaNum">
        update lol_serianum
        set keyvalue=#{keyValue}
        where keyname=#{keyName};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from lol_serianum where keyname=#{keyName};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.lolluckyman.utils.core.QueryParams">
        delete from lol_serianum
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_serianum where keyname=#{keyName};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from lol_serianum where keyname=#{keyName};
    </select>

    <select id="queryCount" parameterType="com.lolluckyman.utils.core.QueryParams" resultType="int">
        select count(*) from lol_serianum
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from lol_serianum
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.lolluckyman.utils.core.QueryParams" resultMap="resultMap">
        select * from lol_serianum
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


